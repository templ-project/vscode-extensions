# Logo Placeholder

Place your logo.png file (128x128 px) here for the extension pack icon.

**Package:** `@{{organization}}/{{language}}-extension-pack`

**Recommended specifications:**
- Size: 128x128 pixels
- Format: PNG
- Background: Transparent or solid color
- Theme: Should work well with both light and dark themes

**Usage:**
1. Create or find a logo that represents {{language}} development
2. Resize it to exactly 128x128 pixels
3. Save it as `logo.png` in this directory
4. Remove this placeholder file

**Design suggestions:**
- Use {{language}} language colors/symbols
- Include subtle IDE branding if desired
- Ensure good contrast for marketplace visibility
- Test appearance in both light and dark VS Code themes
