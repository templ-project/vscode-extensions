# {{displayName}}

{{description}}

## 📦 What's Included

This extension pack includes **{{totalExtensions}} carefully selected extensions** to enhance your {{language}} development experience in {{ide}}.

{{#if requiredExtensions}}
### ✅ Core Extensions ({{requiredExtensions.length}})

These extensions are essential for {{language}} development:

{{#each requiredExtensions}}
- **[{{name}}]({{marketplace_url}})** - {{description}}
{{/each}}
{{/if}}

{{#if optionalExtensions}}
### 💡 Additional Extensions ({{optionalExtensions.length}})

These extensions provide extra functionality and convenience:

{{#each optionalExtensions}}
- **[{{name}}]({{marketplace_url}})** - {{description}}
{{/each}}
{{/if}}

## 🚀 Installation

### Method 1: Install from Marketplace
1. Open {{capitalizedIde}}
2. Go to Extensions view (`Ctrl+Shift+X` / `Cmd+Shift+X`)
3. Search for "{{displayName}}"
4. Click "Install"

### Method 2: Install via Command Line
```bash
{{cliCommand}} --install-extension @templ-project/{{language}}-extension-pack
```

### Method 3: Install from VSIX
1. Download the latest `.vsix` file from [Releases]({{repositoryUrl}}/releases)
2. Open {{capitalizedIde}}
3. Run `Extensions: Install from VSIX...` command
4. Select the downloaded file

{{#if settings}}
## ⚙️ Configuration

After installation, you may want to configure some settings for optimal {{language}} development:

{{#each settings}}
### {{@key}}
```json
{
  "{{@key}}": {{{json value}}}
}
```
> {{description}}

{{/each}}
{{/if}}

{{#if keybindings}}
## ⌨️ Recommended Keybindings

{{#each keybindings}}
- **{{description}}**: `{{key}}`
{{/each}}
{{/if}}

## 📝 Extension Details

| Extension | Publisher | Description |
|-----------|-----------|-------------|
{{#each allExtensions}}
| [{{name}}]({{marketplace_url}}) | {{publisher}} | {{description}} |
{{/each}}

## 🏷️ Categories

{{#each tags}}
`{{this}}` {{/each}}

## 📄 License

### Extension Pack License
This extension pack is licensed under the **MIT License** - see [LICENSE.md]({{repositoryUrl}}/blob/main/packages/{{ide}}/{{language}}/LICENSE.md) for details.

### Third-Party Extension Licenses
**Important**: Each extension included in this pack has its own license terms. {{organization}} is not responsible for the licensing, functionality, or security of third-party extensions.

| Extension | Publisher | License | Description |
|-----------|-----------|---------|-------------|
{{#each allExtensions}}
| [{{name}}]({{#if marketplace_url}}{{marketplace_url}}{{else}}https://marketplace.visualstudio.com/items?itemName={{id}}{{/if}}) | {{#if publisher}}{{publisher}}{{else}}{{getPublisher id}}{{/if}} | {{#if license}}{{license}}{{else}}See extension page{{/if}} | {{description}} |
{{/each}}

### Disclaimer
- We **do not guarantee** the functionality, security, or compatibility of included extensions
- We **are not responsible** for any issues caused by third-party extensions
- Users install and use extensions **at their own risk**
- Please review each extension's license and privacy policy before use

## 🤝 Contributing

Found an issue or want to suggest an extension? Please [open an issue]({{repositoryUrl}}/issues) or submit a pull request.

## 📊 Extension Pack Stats

- **Total Extensions**: {{totalExtensions}}
- **Required Extensions**: {{requiredExtensions.length}}
- **Optional Extensions**: {{optionalExtensions.length}}
- **Target IDE**: {{ide}}
- **Language Focus**: {{language}}

---

*This extension pack is maintained by [{{organization}}](https://github.com/{{organization}}) and updated regularly to include the most useful {{language}} development extensions.*
