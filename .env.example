# GitHub token for API access
# Get your token from: https://github.com/settings/tokens
# Required scopes: repo, issues
GH_TOKEN=your_github_token_here

# Optional: GitHub CLI authentication
# If you prefer using gh CLI, run: gh auth login
# GH_TOKEN will be used as fallback if gh auth fails

GH_OWNER=templ-project
GH_PROJECT=@templ

CWAI_SPECS_FOLDER=specs
CWAI_ISSUE_MANAGER=localfs
